# Dates and times

Convenience functions related to `java.time`. Needs re-thinking and conversion to generic (<code>defmulti</code>) functions so that functions can be used with arbitrary date and time objects.